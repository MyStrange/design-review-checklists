# Сторонних зависимостей нет — проект работает на стандартной библиотеке Python 3.
# Файл оставлен намеренно: шаг установки в GitHub Actions остаётся валидным,
# и сюда легко добавить пакет, если он понадобится в будущем.
